version: "3.7"

services:
  fastapi_app:
    build:
      context: ./fastapi_app
      dockerfile: Dockerfile
    container_name: FastApi_app
    command: uvicorn fastapi_app.main:app --host 0.0.0.0 --port 5001
    ports:
      - "5001:5001"
