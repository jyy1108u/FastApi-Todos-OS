version: "3.7"

services:
  fastapi-app:
    build:
      context: ./fastapi-app
      dockerfile: Dockerfile
    container_name: FastApi-app
    command: uvicorn app.main:app --host 0.0.0.0 --port 5001
    ports:
      - "5001:5001"
